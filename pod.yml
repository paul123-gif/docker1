apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: paul1199/node-app:tagVersion
      ports:
        - containerPort: 8080
